<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tan_background"
    android:orientation="vertical"
    tools:context="com.example.musicalstructureapp.MainActivity">

    <ImageView
        android:id="@+id/now_playing_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:scaleType="centerCrop"
        android:src="@drawable/nowplaying" />

    <TextView
        android:id="@+id/current_song_playing"
        style="@style/NowPlayingStyle"
        android:layout_height="0dp"
        android:layout_weight="3"
        android:background="@color/now_playing"
        android:text="@string/now_playing" />

    <TextView
        android:id="@+id/main_menu"
        style="@style/MainMenuStyle"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/song_library"
        android:text="@string/main_menu" />

</LinearLayout>